= Prompt de reprise — Session 043

__
**EPIC** : EPIC 2 (build v0.1.7 release) + EPIC 8 (fix Dockerfile Docker Hub) + EPIC 5 (XFCE polishment)
**Statut** : Session 042 terminee — fix ffmpeg newline, EPIC 9 (VS Code + gh CLI) implemente, tag v0.1.7 pousse
__

'''

== Session 042 — Resume

- Fix ffmpeg : ajout newline final dans `tools.list.chroot` (cause racine)
- EPIC 9 : VS Code dans `/opt/VSCode-linux-x64/` + gh CLI dans `/opt/gh/`
- Bump `VERSION` → `0.1.7`, tag pousse
- Build #25088165017 en cours : ISO vert, upload artifact/SourceForge en cours

== Session 043 — Priorites

=== 1. Monitorer build #25088165017

* Verifier si upload artifact + SourceForge se terminent correctement
* Si echec, analyser les logs

=== 2. EPIC 8 — Fix Dockerfile Docker Hub

* Job `docker-hub` fail sur installation `xh` (grep -Po invalide)
* Corriger le Dockerfile ou le job CI

=== 3. EPIC 5 (si temps) — Polishment XFCE

* Verifier menu XFCE apres boot QEMU
* Verifier les .desktop entries (VS Code, etc.)

=== 4. EPIC 3 — A/B partition setup CI

* Le test isoTestAB est skipped en CI (runner non-privilegie)
* Verifier si le mock test suffit ou si besoin de vrai test

== Regles

* Jamais de commit sans permission
* Racine de travail = magic_stick/
* Zero sudo, zero build local (CI/CD uniquement)
* INTERDICTION DE LOG LES TOKENS
* FICHIERS PERSONNELS : NE JAMAIS modifier ~/.*
