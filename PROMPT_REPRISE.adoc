= Prompt de reprise — Session 031

__
**EPIC** : EPIC 5 (finalisation XFCE menu + fond ecran) + EPIC 2 (monitor build v0.1.7)
**Statut** : Session 030 terminee — Fix pnpm rate-limit + Docker Hub CI/CD + Blog nav retire
**Mission** : Monitorer build v0.1.7 + EPIC 5 XFCE finalisation
__

'''

== Session 030 — Resume

**Date** : 28 avril 2026
**Resultat** :
- Fix pnpm rate-limit : install via nvm/npm au lieu du binaire standalone GitHub
- Fix Dockerfile CLI : SDKMAN `&&` au lieu de `&` (syntaxe shell)
- CI/CD Docker Hub : job build+push ajoute dans `build.yml` (QEMU, Buildx, cache GHA)
- Site JBake : suppression menu Blog (utilisateur : pas utile vs Doc)
- Procedure de fin de session executee (archive, PROMPT_REPRISE, HISTORY, INDEX, SCRIPT_VERIFICATION, COMPLETED_TASKS_ARCHIVE)

'''

== Session 031 — Priorites

=== 1. Monitorer build v0.1.7

* Pusher tag v0.1.7 et verifier CI/CD vert
* Valider isoTestSoftware passe (pnpm via npm trouve)
* Valider Docker Hub push via job `docker-hub`

=== 2. EPIC 5 — Finalisation XFCE

* Menu Applications personnalise (xml de menu XFCE)
* Fond d'ecran custom Magic Stick (generer image PNG 1920x1080)

=== 3. Backlog

* CACHE-BUILD : issue BACKLOG, a implementer
* EPIC 7 (Auto-update doc + IA release notes) : polish, non critique

'''

== Regles

* Jamais de commit sans permission
* Pas de commandes destructrices sans permission
* Racine de travail = `magic_stick/`
* **Zero sudo sur l'hote** : tout via CI/CD ou conteneur Docker
* **INTERDICTION DE LOG LES TOKENS** — cf AGENT.adoc
* **Zero build local** : GitHub Actions uniquement, lire les logs, corriger, push, reiterer
* **SourceForge** : rsync via SSH, pas d'API S3
* **Boot test** : QEMU+VNC uniquement
* **A/B test** : loopback device en Docker privileged uniquement

