= PROMPT REPRISE — magic-stick Session 074
:date: 2026-05-23

== Mission en cours

EPIC V-1 — Validation Physique ISO → v0.1.14.
Session 073 : Fix initrd patching dans test-boot.sh.
`patch_initrd()` ne depend plus de `file -b` (pas dispo dans conteneur Docker).
Boucle candidates decompresseurs (zstd/zcat/xz/lz4/cat) + validation cpio magic.
Commit 854c8e8 pousse sur main.

== Derniere action

Session 073 (cette session) : fix `patch_initrd()` dans `scripts/test-boot.sh`.
Suppression dependance `file -b` → boucle for decompresseurs avec validation
cpio magic bytes 070701/070702. Commit `854c8e8` + push.

== A faire

1. Verifier le build CI/CD — https://github.com/cheroliv/magic-stick/actions/
2. Si tests pass : `git tag v0.1.14 && git push origin v0.1.14`
   → declenche SourceForge deploy (checksum + rsync)
3. Si tests encore fail : debug le nouveau FAIL dans le smoke test
4. Telecharger ISO depuis SourceForge
5. Flasher : `sudo ./scripts/flash.sh /dev/sdX`
6. Boot physique + checklist EPIC V-1

== Rappels

. ZERO commit/push sans permission
. Build ISO = 20-30 min
. Deploiement SourceForge uniquement sur tag v*
. `gh` deja auth → utiliser `gh`, pas de token dans les fichiers YAML
. Smoke test = ~5 min QEMU boot (-kernel/-initrd + initrd patched)
