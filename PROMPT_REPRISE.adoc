= Prompt de reprise -- Session 021

__
**EPIC** : EPIC 3 Partition A/B — persistence data survival test
**Statut** : Session 020 terminee — skip isoTestAB CI/CD, mock test OK, doc limitation runners
**Mission** : Valider que les donnees persistence survivent a une mise a jour A/B (local ou CI/CD mock)
__

'''

== Session 020 -- Resume

**Date** : 26 avril 2026
**Resultat** : Skip isoTestAB en CI/CD (`if: false` + commentaire), mock test A/B (syntaxe bash + presence task Gradle), documentation limitation runners dans AB_PARTITION_REFERENCE.adoc + SCRIPT_VERIFICATION.adoc

**Archive** : `.agents/sessions/020-skip-mock-ab-cicd.adoc`

'''

== Session 021 -- Priorites

**Mission** : Valider la survie des donnees persistence lors d'une mise a jour A/B

=== 1. Test local persistence survival

* Utiliser `./gradlew isoTestAB` (local) ou `sudo scripts/test-ab-partition.sh full-test`
* Ecrire un fichier dans la partition persistence
* Lancer un update-system.sh install sur la partition inactive
* Verifier que le fichier persiste
* Ou creer un test dedie `test-persistence.sh`

=== 2. CI/CD mock persistence

* Si loop device impossible : tester sans loop device (mock)
  - Valider que `update-system.sh` ne touche pas partition 3 (persistence)
  - grep/update-system.sh pour verifier aucun mkfs/mount/umount sur persistence

=== 3. Mettre a jour BACKLOG + SCRIPT_VERIFICATION

* Cocher taches EPIC 3 restantes
* Ajouter test persistence a SCRIPT_VERIFICATION.adoc

=== Priorites reportees

* EPIC 7 (Auto-update doc + IA release notes) : reporte
* EPIC 4 (Logiciels Ollama/Docker/SDKMAN) : depend du boot
* EPIC 5 (Customisation XFCE) : cosmetique

'''

== Regles

* Jamais de commit sans permission
* Pas de commandes destructrices sans permission
* Racine de travail = `magic_stick/`
* **Zero sudo sur l'hote** : tout via CI/CD ou conteneur Docker
* **INTERDICTION DE LOG LES TOKENS** — cf AGENT.adoc
* **Zero build local** : GitHub Actions uniquement, lire les logs, corriger, push, reiterer
* **SourceForge** : rsync via SSH, pas d'API S3
* **Boot test** : QEMU+VNC uniquement (cf `.agents/BOOT_TEST_REFERENCE.adoc`)
* **A/B test** : loopback device en Docker privileged uniquement (cf `.agents/AB_PARTITION_REFERENCE.adoc`)
* **CI/CD A/B** : runners non-privileged -> skip ou mock en attendant
