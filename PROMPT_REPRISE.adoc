= Prompt de reprise — Session 027

__
**EPIC** : EPIC 2 (tests reels) + EPIC 5 (XFCE customisation) + EPIC 8 (Dockerisation CLI)
**Statut** : Session 026 terminee — version automatique + release workflow + outils qemu/ripgrep, commit+push
**Mission** : Tester le pipeline release (bump version), traiter les EPICs prioritaires non entames
__

'''

== Session 026 — Resume

**Date** : 27 avril 2026
**Resultat** :
- Creer fichier VERSION (`0.1.3`) source de verite unique
- build.gradle.kts, build.sh lisent la version depuis VERSION
- Modifier build.yml : ISO dynamique, env version partagee, publish conditionnel sur tag
- Creer release.yml : bump semver (patch/minor/major) + tag auto + declenchement build.yml auto
- Ajout qemu-system-x86, qemu-utils, ripgrep dans tools.list.chroot
- Commit + push sur main (`7917747`)

'''

== Session 027 — Priorites

=== 1. Valider le pipeline release (test manuel si pertinent)

* Optionnel : declencher release.yml en patch pour verifier la mecanique end-to-end
* Verifier que build.yml se declenche bien sur le nouveau tag

=== 2. EPIC 2 — Tests sur materiel reel (si hardware disponible)

* Tester boot BIOS sur cle USB reelle (avec flash.sh)
* Tester boot UEFI sur cle USB reelle
* Documenter les resultats dans BOOT_TEST_REFERENCE.adoc

=== 3. EPIC 5 — Personnalisation XFCE

* Theme et fond d'ecran custom (config includes.chroot ou skel)
* Raccourcis clavier standardises
* Panel XFCE preconfigure
* Scripts de demarrage automatique
* Menu Applications personnalise

=== 4. EPIC 8 — Dockerisation CLI (artefact parallele)

* Creer Dockerfile ubuntu:24.04 avec memes outils CLI que le chroot (sans GUI)
* Build et validation locale
* CI/CD Docker Hub (`cheroliv/magic-stick-cli`)
* Documenter dans README

=== Priorites reportees

* EPIC 7 (Auto-update doc + IA release notes) : polish, non critique
* CACHE-BUILD : issue BACKLOG, a implementer
* Full loopback test A/B : Docker privileged uniquement
* Ventoy / script cle en masse : idees TODO, pas prioritaires

'''

== Regles

* Jamais de commit sans permission
* Pas de commandes destructrices sans permission
* Racine de travail = `magic_stick/`
* **Zero sudo sur l'hote** : tout via CI/CD ou conteneur Docker
* **INTERDICTION DE LOG LES TOKENS** — cf AGENT.adoc
* **Zero build local** : GitHub Actions uniquement, lire les logs, corriger, push, reiterer
* **SourceForge** : rsync via SSH, pas d'API S3
* **Boot test** : QEMU+VNC uniquement
* **A/B test** : loopback device en Docker privileged uniquement
* **CI/CD A/B** : runners non-privileged -> skip ou mock en attendant
