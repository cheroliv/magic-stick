= Prompt de reprise — Session 039

__
**EPIC** : EPIC 2 (valider build v0.1.7) + EPIC 9 (VS Code/gh dans /opt/ — implementation) + EPIC 5 (XFCE polishment)
**Statut** : Session 038 terminee — Analyse build squashfs "No space left on device" + relance CI/CD
__

'''

== Session 038 — Resume

**Date** : 28 avril 2026
**Resultat** :
- Constat : commit fix Toolbox deja push en session 037, rien a commiter
- Build ISO #25068590605 FAIL : `No space left on device` pendant creation squashfs
- Build website OK
- Nouveau workflow_dispatch #25070508605 relance
- Procedure de fin de session + commit/push agents

'''

== Session 039 — Priorites

=== 1. Analyser les logs du build #25070508605

* Si termine : `gh run view 25070508605 --log`
* Verdict : vert (espace suffisant cette fois) ou rouge (meme erreur No space)

=== 2. Si build vert → Bump v0.1.7 + livraisons

* Bump VERSION a 0.1.7 + tag v0.1.7 + push tag → declencher release workflow (ISO artifact + SourceForge + Docker Hub)
* Monitorer tous les workflows

=== 3. Si build rouge "No space" → Debug espace disque

* Analyser la taille du chroot dans les logs (avant squashfs)
* Options :
  . Purger les caches apt dans le chroot avant squashfs (`apt-get clean`, `rm -rf /var/lib/apt/lists/*`)
  . Compression squashfs plus agressive (`-comp xz -Xbcj x86`)
  . Verifier si le runner ubuntu-latest a change de taille disque
  . Ajouter un hook chroot de nettoyage pre-squashfs

=== 4. EPIC 9 — Implementation VS Code et gh CLI dans /opt/

* VS Code : hook chroot install tarball dans /opt/VSCode-linux-x64/
  * Script /usr/local/bin/vscode-update.sh pour mise a jour
  * Configs utilisateur : /etc/skel/.config/Code/User/settings.json
  * Custom CSS : /etc/skel/.vscode-custom-css/custom.css
  * Modele : cheroliv.com/blog/2026/0103
* gh CLI : hook chroot install dans /opt/gh/ + symlink /usr/local/bin/gh
  * Modele : binaire standalone depuis GitHub Releases (pattern xh/just existant)
* Ajouter les checks dans scripts/test-software.sh
* Test ISO squashfs via CI/CD

=== 5. EPIC 5 — Polishment XFCE (si build OK)

* Verifier menu XFCE dans l'ISO apres boot QEMU
* Ajuster .directory/.desktop si necessaire

'''

== Backlog — Ce qui reste a faire

==== EPIC 2 : Build ISO
* [ ] Tester le boot BIOS sur cle USB reelle
* [ ] Valider le support UEFI sur hardware reel
* [ ] Tester le boot UEFI sur cle USB reelle

==== EPIC 4 : Logiciels
* [ ] Tester que chaque logiciel fonctionne dans le live boot

==== EPIC 9 : Editeurs de code dans /opt/
* [ ] VS Code dans /opt/
* [ ] gh CLI dans /opt/

==== EPIC 5 : Personnalisation XFCE
* [x] Menu Applications personnalise
* [ ] Theme et fond d'ecran
* [ ] Raccourcis clavier
* [ ] Panel XFCE
* [ ] Scripts demarrage auto

==== EPIC 8 : Dockerisation CLI
* [ ] Build et validation image locale
* [ ] CI/CD Docker Hub
* [ ] Tester push local
* [ ] Tester comme conteneur de job
* [ ] Documenter dans README

==== EPIC 7 : Site de doc auto + IA release notes
* [ ] Auto-updater site de doc
* [ ] Generer notes de version IA

'''

== Regles

* Jamais de commit sans permission
* Pas de commandes destructrices sans permission
* Racine de travail = magic_stick/
* Zero sudo sur l'hote
* INTERDICTION DE LOG LES TOKENS
* Zero build local : GitHub Actions uniquement
* SourceForge : rsync via SSH
* Boot test : QEMU+VNC
* A/B test : loopback device en Docker privileged
* FICHIERS PERSONNELS : NE JAMAIS modifier ~/.gitconfig, ~/.bashrc, ~/.ssh/*, etc.
* EDITEURS : Dans /opt/
