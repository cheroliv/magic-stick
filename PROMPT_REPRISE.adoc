= Prompt de reprise -- Session 013

__
**EPIC** : Build ISO Xubuntu bootable + installable
**Statut** : Session 012 terminee — PATH OpenCode corrige, article blog redige
**Mission** : EPIC 2 via CI/CD — lire les logs, corriger, push, reiterer
__

'''

== Session 012 -- Resume

**Date** : 25 avril 2026
**Resultat** : SUCCES — PATH OpenCode corrige (.zshenv + symlink NVM current + wrapper _nvm). Article blog redige.

**Probleme** : Tous les outils du PATH (.zshrc) etaient invisibles pour OpenCode (shell non-interactif). Solution : .zshenv + symlink `~/.nvm/current`.

**Archive** : `.agents/sessions/012-opencode-path-shell-fix.adoc`

'''

== Session 013 -- Priorites

**Mission** : Construire l'ISO via GitHub Actions uniquement — plus de build local

=== 1. CI/CD EPIC 2 : Build ISO via GitHub Actions (PRIORITE MAXIMALE)

* Lire les logs du dernier workflow run GitHub Actions
* Identifier les erreurs de build ISO
* Corriger les configs/scripts necessaires
* Push et relancer le build
* Iterer jusqu'a ce que l'ISO bootable soit generee

=== 2. Tester le boot de l'ISO generee

* Telecharger l'artefact ISO depuis GitHub Actions
* Tester boot QEMU BIOS avec `-serial stdio`
* Capturer les messages kernel
* Si blocage initramfs : debug via logs serial

=== 3. Apres boot OK : Ajouter support UEFI

* Creer hook `999-add-efi-support.binary`
* Utiliser grub-mkimage pour creer l'image EFI
* Ajouter EFI a l'ISO via xorriso
* Tester boot UEFI avec QEMU

=== Criteres d'Acceptation

* [ ] ISO bootable generee via CI/CD
* [ ] ISO bootable en BIOS (QEMU OK)
* [ ] ISO bootable en UEFI
* [ ] Workflow CI/CD vert

'''

== Regles

* Jamais de commit sans permission
* Pas de commandes destructrices sans permission
* Racine de travail = `magic_stick/`
* **Zero sudo sur l'hote** : tout via CI/CD ou conteneur Docker
* **INTERDICTION DE LOG LES TOKENS** — cf AGENT.adoc
* **Zero build local** : GitHub Actions uniquement, lire les logs
* Archive + PROMPT_REPRISE mis a jour en fin de session