= Prompt de reprise — Session 052

__
**EPIC** : EPIC 11 (validation reproductibilite CI/CD) + taches en attente
**Statut** : Session 051 terminee — Dockerfile CLI synchronise avec versions.sh + doc convergence AGENT.adoc. CI/CD a declencher.
__

'''

== Session 051 — Resume

- EPIC 11 suite realise :
  * P0.1 `docker/magic-stick-cli/Dockerfile` ARG synchronises avec `versions.sh` :
    ** ARG centralises au top (PYTHON_VERSION, UV_VERSION, NVM_VERSION, NODE_VERSION, PNPM_VERSION, LAZYGIT_VERSION, XH_VERSION, JUST_VERSION)
    ** ARG dupliques supprimes (3 lignes), uv pinne via tarball (plus de `install.sh` latest)
    ** NVM_VERSION/PNPM_VERSION references dynamiques (plus de hardcode inline)
    ** Label bump v0.1.12
  * P0.2 Doc convergence chroot/Dockerfile ajoutee dans `AGENT.adoc`
  * P0.3 CI/CD NON lance — commit + push a faire en fin de session 051

== Plan Session 052 — Priorites

=== 1. EPIC 11 — Validation reproductibilite CI/CD

* [ ] Valider reproductibilite via CI/CD (commit de la fin de session 051, attendre build ISO, verifier versions.sh inclus)
* [ ] Si CI/CD vert → passer aux taches suivantes

=== 2. EPIC 11 suite — Taches en attente (P0.4-P0.7)

* [ ] P0.4 Mettre a jour BACKLOG.adoc (arch.sh deja cree en 050, tache 233 obsolete)
* [ ] P0.5 Ajouter traps EXIT dans `update-system.sh` et `build-inner.sh`
* [ ] P0.6 Checksums SHA256 des binaires dans les hooks chroot
* [ ] P0.7 CACHE-BUILD acceleration CI/CD

=== 3. EPIC 5 — Validation boot hardware (si utilisateur a flashe)

* [ ] Test boot BIOS sur cle USB reelle
* [ ] Test boot UEFI sur cle USB reelle
* [ ] Valider autostart XFCE (`magic-stick-welcome.sh`)

=== 4. EPIC 3 / EPIC 6 (si temps)

* [ ] Valider `update-system.sh` avec trap en production sur cle USB
* [ ] Tester rollback A/B sur cle USB

== Regles

* Commit/push/tag : je suis seul sur le projet, pas de contrainte permission
* Zero sudo, zero build local (CI/CD uniquement)
* INTERDICTION DE LOG LES TOKENS
* FICHIERS PERSONNELS : NE JAMAIS modifier ~/.*
