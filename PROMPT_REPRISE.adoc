= Prompt de reprise -- Session 020

__
**EPIC** : EPIC 3 Partition A/B — skip CI/CD + mock test + doc limitation runners
**Statut** : Session 019 terminee — Dockerfile corrigé, test-boot VNC OK, A/B CI/CD bloqué
**Mission** : Adapter isoTestAB pour CI/CD, ajouter mock test A/B, documenter limitation runners
__

'''

== Session 019 -- Resume

**Date** : 26 avril 2026
**Resultat** : Dockerfile corrigé (CMD bash + WORKDIR), test-boot.sh --vnc validé (localhost:6080 accessible), A/B en CI/CD bloqué car runners non-privileged

**Archive** : `.agents/sessions/019-correction-docker-validation-boot.adoc`

'''

== Session 020 -- Priorites

**Mission** : Adapter le test A/B pour la CI/CD (a -> b -> c)

=== 1. (a) Skip isoTestAB en CI/CD

* Commenter/supprimer la step `Test A/B partition setup` dans `.github/workflows/build.yml`
* Ou ajouter `if: false` / condition pour skip en attendant
* Verifier que le build CI/CD reste vert (Build + Verify + Test boot)

=== 2. (b) Ajouter un mock test A/B en CI/CD

* Creer un test sans loop device (validation du script + syntaxe + logique)
* Ou utiliser un simple `bash -n` + `test --help` pour valider la presence du script
* Alternative : tester `update-system.sh` avec des arguments mock (dry-run)

=== 3. (c) Documenter la limitation runners self-hosted

* Mettre a jour `AB_PARTITION_REFERENCE.adoc` ou `BACKLOG.adoc`
* Indiquer que le test A/B complet necessite un runner self-hosted privileged
* Garder `isoTestAB` disponible en local (`./gradlew isoTestAB`)

=== 4. Nettoyage et verification

* Verifier syntaxe des workflows YAML
* Test local `./gradlew isoVerify isoTestBoot` (sans isoTestAB)
* Mettre a jour `SCRIPT_VERIFICATION.adoc` avec les decisions a/b/c

=== Priorites reportees

* EPIC 7 (Auto-update doc + IA release notes) : reporte
* EPIC 4 (Logiciels Ollama/Docker/SDKMAN) : depend du boot
* EPIC 5 (Customisation XFCE) : cosmetique

'''

== Regles

* Jamais de commit sans permission
* Pas de commandes destructrices sans permission
* Racine de travail = `magic_stick/`
* **Zero sudo sur l'hote** : tout via CI/CD ou conteneur Docker
* **INTERDICTION DE LOG LES TOKENS** — cf AGENT.adoc
* **Zero build local** : GitHub Actions uniquement, lire les logs, corriger, push, reiterer
* **SourceForge** : rsync via SSH, pas d'API S3
* **Boot test** : QEMU+VNC uniquement (cf `.agents/BOOT_TEST_REFERENCE.adoc`)
* **A/B test** : loopback device en Docker privileged uniquement (cf `.agents/AB_PARTITION_REFERENCE.adoc`)
* **CI/CD A/B** : runners non-privileged → skip ou mock en attendant
