= Prompt de reprise — Session 044

__
**EPIC** : EPIC 8 (fix Dockerfile Docker Hub) + EPIC 5 (XFCE polishment) + EPIC 3 (A/B partition setup CI)
**Statut** : Session 043 terminee — build v0.1.7 VERIFIE (vert sur main #25091043201), EPIC 8 en attente de fix, EPIC 5/3 non demarres
__

'''

== Session 043 — Resume

- Build #25091043201 : **VERT** (22m22s, ISO 6.6G, verify/boot/software/mock AB OK)
- Upload artifact + SourceForge : SKIPPED (push `main`, pas un tag `v*` — attendu)
- docker-hub job : SKIPPED (idem)
- EPIC 8 non traite dans cette session
- Node.js 20 deprecation notice (non bloquant, echéance juin 2026)

== Session 044 — Priorites

=== 1. EPIC 8 — Fix Dockerfile Docker Hub

* **Cause** : job `docker-hub` fail sur `grep -Po` invalide pour extraction version `xh`
* **Fichier** : `docker/magic-stick-cli/Dockerfile`
* **Action** : corriger le `grep -Po` (pattern Perl non supporté par grep busybox/gnu) ou remplacer par un parsing robuste (sed/awk/curl + jq)
* **Verification** : build local `docker build` ou CI/CD vert

=== 2. Monitorer build post-fix EPIC 8

* Push du fix sur `main`
* Verifier que le job `docker-hub` ne skippe plus (si build.yml conditionne par `if: startsWith(github.ref, 'refs/tags/v')`)
* Eventuellement forcer un tag `v0.1.8` si EPIC 8 corrige un bug bloquant

=== 3. EPIC 5 (si temps) — Polishment XFCE

* Verifier menu XFCE apres boot QEMU
* Verifier les .desktop entries (VS Code, Open-WebUI, AI Assistants)
* Fond d'ecran custom si non present

=== 4. EPIC 3 — A/B partition setup CI

* Le test isoTestAB est skipped en CI (runner non-privileged)
* Verifier si le mock test suffit ou si besoin de vrai test avec runner self-hosted

== Regles

* Jamais de commit sans permission
* Racine de travail = magic_stick/
* Zero sudo, zero build local (CI/CD uniquement)
* INTERDICTION DE LOG LES TOKENS
* FICHIERS PERSONNELS : NE JAMAIS modifier ~/.*
