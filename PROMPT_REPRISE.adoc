= Prompt de reprise — Session 038

__
**EPIC** : EPIC 2 (valider build v0.1.7) + EPIC 9 (VS Code/gh dans /opt/ — implementation) + EPIC 5 (XFCE polishment)
**Statut** : Session 037 terminee — Fix JetBrains Toolbox 404 + EPIC 9 REVERSAL /opt/
__

'''

== Session 037 — Resume

**Date** : 28 avril 2026
**Resultat** :
- Fix JetBrains Toolbox fallback URL : `jetbrains-toolbox-latest.tar.gz` → 404, remplace par fallback statique `3.4.3.81140`
- EPIC 9 REVERSAL : VS Code et gh CLI retournent dans `/opt/` (pas `/home/user/apps/`), docs mises a jour
- Commit + push NON effectues en session — a faire en session 038
'''

== Session 038 — Priorites

=== 1. Commit + push le fix JetBrains Toolbox

* Committer les 3 fichiers modifies : `060-install-chroot-software.chroot`, `BACKLOG.adoc`, `PROMPT_REPRISE.adoc`
* Pusher → declencher CI/CD build.yml automatiquement
* Monitorer le nouveau build ISO (run GitHub Actions)

=== 2. Si build vert → Bump v0.1.7 + livraisons

* Bump VERSION a 0.1.7 + tag v0.1.7 + push tag → declencher release workflow
* ISO SourceForge : rsync de `build/magic_stick_0.1.7.iso`
* Image Docker Hub : push image magic-stick-cli

=== 3. Si build rouge → Analyser les logs CI/CD

* Corriger les CHECKS restants
* Reiterer commit/push jusqu'a build vert

=== 4. EPIC 9 — Implementation VS Code et gh CLI dans /opt/

* VS Code : hook chroot install tarball dans `/opt/VSCode-linux-x64/`
  * Script `/usr/local/bin/vscode-update.sh` pour mise a jour
  * Configs utilisateur : `/etc/skel/.config/Code/User/settings.json`
  * Custom CSS : `/etc/skel/.vscode-custom-css/custom.css`
  * Modele : cheroliv.com/blog/2026/0103
* gh CLI : hook chroot install dans `/opt/gh/` + symlink `/usr/local/bin/gh`
  * Modele : binaire standalone depuis GitHub Releases (pattern xh/just existant)
* Ajouter les checks dans `scripts/test-software.sh`
* Test ISO squashfs via CI/CD

=== 5. EPIC 5 — Polishment XFCE (si build OK)

* Verifier menu XFCE dans l'ISO apres boot QEMU
* Ajuster .directory/.desktop si necessaire

'''

== Backlog — Ce qui reste a faire

==== EPIC 2 : Build ISO
* [ ] Tester le boot BIOS sur cle USB reelle
* [ ] Valider le support UEFI sur hardware reel
* [ ] Tester le boot UEFI sur cle USB reelle

==== EPIC 4 : Logiciels
* [ ] Tester que chaque logiciel fonctionne dans le live boot (Docker, Podman, Ollama, NVM, pnpm, Python, uv, SDKMAN+JDK25, ripgrep, fd, fzf, lazygit, xh, httpie, just, flatpak, JetBrains Toolbox, zsh, starship, opencode, graphify, ffmpeg)

==== EPIC 9 : Editeurs de code dans /opt/ (REVERSAL session 037)
* [ ] VS Code : installation tarball dans /opt/VSCode-linux-x64/ + /usr/local/bin/vscode-update.sh + configs skel
* [ ] gh CLI : installation dans /opt/gh/ + symlink /usr/local/bin/gh

==== EPIC 5 : Personnalisation XFCE
* [x] Menu Applications personnalise (FAIT session 033)
* [ ] Theme et fond d'ecran custom (decision : garder xubuntu-blue.png par defaut)
* [ ] Raccourcis clavier standardises (XML skel deja en place — a verifier)
* [ ] Panel XFCE preconfigure (XML skel fait, a verifier)
* [ ] Scripts de demarrage automatique

==== EPIC 8 : Dockerisation CLI
* [ ] Build et validation de l'image en local (docker build, docker run -it)
* [ ] CI/CD Docker Hub : reevaluer build.yml pour utiliser secrets.DOCKERHUB_TOKEN
* [ ] Tester le push local : executer scripts/dockerhub-push.sh
* [ ] Tester l'image comme conteneur de job GitHub Actions factice
* [ ] Documenter l'usage dans le README

==== EPIC 7 : Site de doc auto + IA release notes
* [ ] Auto-updater le site de doc lors d'un deploiement
* [ ] Generer les notes de version via IA lors d'un release

'''

== Regles

* Jamais de commit sans permission
* Pas de commandes destructrices sans permission
* Racine de travail = `magic_stick/`
* **Zero sudo sur l'hote** : tout via CI/CD ou conteneur Docker
* **INTERDICTION DE LOG LES TOKENS** — cf AGENT.adoc
* **Zero build local** : GitHub Actions uniquement, lire les logs, corriger, push, reiterer
* **SourceForge** : rsync via SSH, pas d'API S3
* **Boot test** : QEMU+VNC uniquement
* **A/B test** : loopback device en Docker privileged uniquement
* **FICHIERS PERSONNELS** : NE JAMAIS modifier `~/.gitconfig`, `~/.bashrc`, `~/.ssh/*`, etc.
* **EDITEURS** : Dans `/opt/` comme le reste de la stack systeme (REVERSAL session 037)
