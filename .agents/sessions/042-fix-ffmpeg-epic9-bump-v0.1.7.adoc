= Session 042 — Fix ffmpeg newline + EPIC 9 (VS Code / gh CLI) + Bump v0.1.7

== Resume

Session 042 avait deux objectifs issus du PROMPT_REPRISE :
1. Corriger le FAIL CI "ffmpeg NOT found" du build #25086417841
2. Implementer EPIC 9 : VS Code + gh CLI dans /opt/

=== Cause racine ffmpeg

Le build #25086417841 a echoue au test software avec `[FAIL] ffmpeg NOT found`.
Analyse : ffmpeg etait present dans `tools.list.chroot` (ligne 48) mais la derniere ligne ne se terminait PAS par un newline `\n`.
Consequence : live-build ignore la derniere ligne si elle ne se termine pas par un newline.

=== Fix ffmpeg

Modification : `config/live-build/package-lists/tools.list.chroot`
- Ajout d'un `\n` final apres `ffmpeg`

=== EPIC 9 : VS Code + gh CLI

Ajout dans `config/live-build/hooks/060-install-chroot-software.chroot` :
- VS Code : telecharge le tarball stable linux-x64 dans `/opt/VSCode-linux-x64/`, symlink `/usr/local/bin/code`, desktop entry XFCE
- gh CLI : telecharge le dernier binaire GitHub Releases dans `/opt/gh/`, symlink `/usr/local/bin/gh`

Tests CI/CD : ajout de `check_binary "code"` et `check_binary "gh"` dans `scripts/test-software.sh`

=== Bump v0.1.7
- Modification `VERSION` → `0.1.7`
- `git tag v0.1.7 && git push origin v0.1.7`

=== Builds CI/CD
| Build | Statut | Details |
| #25087617069 | ✅ Vert complet | fix ffmpeg + EPIC 9 inclus, 21 min |
| #25088165017 | 🟡 Build ISO vert, Docker Hub fail | Tag v0.1.7, build ISO OK, test software OK, upload en cours |

=== Fichiers modifies
- `config/live-build/package-lists/tools.list.chroot` (fix newline)
- `config/live-build/hooks/060-install-chroot-software.chroot` (VS Code + gh CLI)
- `scripts/test-software.sh` (checks code + gh)
- `VERSION` (bump 0.1.7)

=== Score
7.5/10 — Fix rapide et cible (newline), EPIC 9 bien isole, bump version propre.
Points perdus : Docker Hub encore fail (xh Dockerfile, non critique pour l'ISO).
